#' @param family String - family of distribution. Must be in  \code{c("gaussian", "poisson", "gamma", "tweedie", "binomial")}
#' @param tweedie_power Numeric - power of tweedie distribution if chosen in \code{family}
